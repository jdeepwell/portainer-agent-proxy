Look at Spec.md to see the current specifications for the project.

Look at Status.md to see the current status of the project.

When asked to via "update status", update the Status.md file with all changes made to the project since the last update. To NOT make Status.md a list of changes, but rather a summary of the current state of the project. So change/add things in Status.md to reflect the current state of the project.

Do not commit anything to the repo unless asked to via "commit changes". When asked to commit changes, commit all changes made to the project since the last commit. IMPORTANT: If some files are aleady staged, only commit those files that are staged with a commit message reflecting the changes made to those files. If no files are staged, commit all changes with a commit message reflecting the changes made to all files.

When asked to via "update spec", update the Spec.md file with all changes talked about in the current conversation. Do NOT make Spec.md a list of changes, but rather change/add things in Spec.md to reflect the current specifications for the project.

This project is to be hosted on GitHub under the repository [portainer-agent-proxy](https://github.com/jdeepwell/portainer-agent-proxy)
 